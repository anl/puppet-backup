#!/bin/bash
<% targets.each do |node, dirs| -%>

<% dirs.each do |dir| -%>
rsync -e "ssh -i <%= ssh_dir %>/backup-rsync.pem" -avz root@<%= node %>:<%= dir %> <%= backup_home %>/<%= node %><%= dir %>
<% end -%>
<% end -%>
